#include "main.h"

/**
 * _abc - computes the absolute value of an interger
 * @c: The number to be computed
 * Return: Absolute value of number or zero
 */

int _abc(int c)
{ 

	if (c < 0)
	{
		int abs_val;

		abc_val = c * -1;
		return (abs_val);
	}
	return (c);
}
